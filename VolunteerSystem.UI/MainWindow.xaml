<Window x:Class="VolunteerSystem.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:VolunteerSystem.UI.ViewModels"
        xmlns:v="clr-namespace:VolunteerSystem.UI.Views"
        mc:Ignorable="d"
        Title="Volunteer Management System" Height="700" Width="1000">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <v:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RegistrationViewModel}">
            <v:RegistrationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:VolunteerDashboardViewModel}">
            <v:VolunteerDashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:OrganizerDashboardViewModel}">
            <v:OrganizerDashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AdminDashboardViewModel}">
            <v:AdminDashboardView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentView}"/>
    </Grid>
</Window>
