<UserControl x:Class="VolunteerSystem.UI.Views.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#f0f2f5">
        <Border Background="White" CornerRadius="10" Padding="30" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="1" Opacity="0.2"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Join Us" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                
                <TextBlock Text="I am a:"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                    <RadioButton Content="Volunteer" IsChecked="{Binding IsVolunteer}" GroupName="Role" Margin="0,0,15,0"/>
                    <RadioButton Content="Organizer" IsChecked="{Binding IsOrganizer}" GroupName="Role"/>
                </StackPanel>

                <TextBlock Text="Full Name"/>
                <TextBox Text="{Binding FullName}" Margin="0,5,0,10" Padding="5"/>

                <TextBlock Text="Email"/>
                <TextBox Text="{Binding Email}" Margin="0,5,0,10" Padding="5"/>

                <TextBlock Text="Password"/>
                <PasswordBox x:Name="RegPasswordBox" PasswordChanged="RegPasswordBox_PasswordChanged" Margin="0,5,0,10" Padding="5"/>

                <!-- Volunteer Fields -->
                <StackPanel Visibility="{Binding IsVolunteer, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Skills (comma separated)"/>
                    <TextBox Text="{Binding Skills}" Margin="0,5,0,10" Padding="5"/>
                </StackPanel>

                <!-- Organizer Fields -->
                <StackPanel Visibility="{Binding IsOrganizer, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Organization Name"/>
                    <TextBox Text="{Binding OrganizationName}" Margin="0,5,0,10" Padding="5"/>
                </StackPanel>

                <Button Content="Register" Command="{Binding RegisterCommand}" Margin="0,10,0,5" Padding="10,5" Background="#28a745" Foreground="White"/>
                
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,5"/>

                <Button Content="Back to Login" Command="{Binding NavigateToLoginCommand}" Margin="0,10,0,0" Background="Transparent" BorderThickness="0" Foreground="#007bff"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
