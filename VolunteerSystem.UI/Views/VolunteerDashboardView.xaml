<UserControl x:Class="VolunteerSystem.UI.Views.VolunteerDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#007bff" Padding="20">
            <Grid>
                <TextBlock Text="{Binding WelcomeMessage}" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="Logout" Command="{Binding LogoutCommand}" HorizontalAlignment="Right" Padding="10,5" Background="White" Foreground="#007bff"/>
            </Grid>
        </Border>

        <!-- Content -->
        <TabControl Grid.Row="1" Margin="10">
            <TabItem Header="Opportunities">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox Width="300" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                        <Button Content="Search" Margin="10,0,0,0" Padding="10,5"/>
                    </StackPanel>
                    
                    <ListView Grid.Row="1" ItemsSource="{Binding Opportunities}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#ddd" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Foreground="#333"/>
                                        <TextBlock Text="{Binding Organizer.OrganizationName}" Foreground="#666" Margin="0,2,0,5"/>
                                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                        
                                        <TextBlock Text="Events:" FontWeight="SemiBold" Margin="0,5,0,2"/>
                                        <ItemsControl ItemsSource="{Binding Events}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="0,2">
                                                        <TextBlock Text="{Binding StartTime, StringFormat='{}{0:g}'}"/>
                                                        <TextBlock Text=" - "/>
                                                        <TextBlock Text="{Binding Location}"/>
                                                        <Button Content="Apply" Command="{Binding DataContext.ApplyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="10,0,0,0" Padding="5,2" Background="#28a745" Foreground="White" BorderThickness="0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </TabItem>
            <TabItem Header="My Calendar">
                <TextBlock Text="Calendar Placeholder" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </TabItem>
            <TabItem Header="Profile">
                <TextBlock Text="Profile Placeholder" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
