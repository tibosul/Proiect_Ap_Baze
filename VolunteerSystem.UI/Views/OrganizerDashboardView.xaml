<UserControl x:Class="VolunteerSystem.UI.Views.OrganizerDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#6f42c1" Padding="20">
            <Grid>
                <TextBlock Text="{Binding WelcomeMessage}" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="Logout" Command="{Binding LogoutCommand}" HorizontalAlignment="Right" Padding="10,5" Background="White" Foreground="#6f42c1"/>
            </Grid>
        </Border>

        <!-- Content -->
        <TabControl Grid.Row="1" Margin="10">
            <TabItem Header="My Opportunities">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <!-- List -->
                    <ListView ItemsSource="{Binding MyOpportunities}" Margin="0,0,10,0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="5">
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Location, StringFormat='Location: {0}'}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Events.Count, StringFormat='Events: {0}'}" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <Button Content="Add Event" Command="{Binding DataContext.AddEventCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="5,2" Background="#17a2b8" Foreground="White" BorderThickness="0"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Create Form -->
                    <Border Grid.Column="1" BorderBrush="#ddd" BorderThickness="1" CornerRadius="5" Padding="10" VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="Create New Opportunity" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <TextBlock Text="Title"/>
                            <TextBox Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
                            
                            <TextBlock Text="Description"/>
                            <TextBox Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}" Height="60" TextWrapping="Wrap" Margin="0,5,0,10"/>
                            
                            <TextBlock Text="Location"/>
                            <TextBox Text="{Binding NewLocation, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                            <Button Content="Create" Command="{Binding CreateCommand}" Background="#28a745" Foreground="White" Padding="10,5"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            <TabItem Header="Events">
                <TextBlock Text="Event Management Placeholder" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
