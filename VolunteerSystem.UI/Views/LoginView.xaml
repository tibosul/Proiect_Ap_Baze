<UserControl x:Class="VolunteerSystem.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#f0f2f5">
        <Border Background="White" CornerRadius="10" Padding="30" Width="300" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="1" Opacity="0.2"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Volunteer System" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                
                <TextBlock Text="Email"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" Padding="5"/>

                <TextBlock Text="Password"/>
                <!-- Using TextBox for simplicity in this blind implementation or PasswordBox with code behind -->
                <PasswordBox x:Name="UserPasswordBox" PasswordChanged="UserPasswordBox_PasswordChanged" Margin="0,5,0,10" Padding="5"/>

                <Button Content="Login" Command="{Binding LoginCommand}" Margin="0,10,0,5" Padding="10,5" Background="#007bff" Foreground="White"/>
                
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,5"/>

                <Button Content="Create an Account" Command="{Binding NavigateToRegisterCommand}" Margin="0,10,0,0" Background="Transparent" BorderThickness="0" Foreground="#007bff"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
